# 📋 ระบบจัดการงานแบบเรียลไทม์ (Real-time Collaborative To-Do List)

เว็บแอปพลิเคชันสำหรับจัดการรายการสิ่งที่ต้องทำ (To-Do List) ที่ออกแบบมาให้ผู้ใช้งานหลายคนทำงานร่วมกันได้ในเวลาเดียวกัน ข้อมูลจะอัปเดตหากันทันที (Real-time) โดยไม่ต้องกดรีเฟรชหน้าจอ เหมาะสำหรับการทำงานเป็นทีม

---

## 🔵 ทดลองใช้งานจริง (Live Demo)
สามารถเข้าใช้งานได้ทันทีผ่านลิงก์นี้ (ไม่ต้องติดตั้ง):

👉 **[https://mytodolist-qo86.onrender.com/]**

---

## ✨ คุณสมบัติเด่น (Features)
- 🔄 **Real-time Synchronization: เมื่อมีคนเพิ่ม, ลบ, หรือแก้ไขงาน หน้าจอของทุกคนจะเปลี่ยนตามทันที (ใช้เทคโนโลยี Socket.io)
- 📊 **Multi-Status Tracking: เปลี่ยนสถานะงานได้ 3 ระดับ (⏳ รอดำเนินการ, 🚧 กำลังทำ, ✅ เสร็จสิ้น) พร้อมสีแยกชัดเจน
- 📝 **Edit & Delete: แก้ไขข้อความและลบงานได้ง่ายๆ ด้วยปุ่มที่ออกแบบมาให้ใช้งานสะดวก (สีเหลือง/แดง)
- 👤 **User Identification: ระบบระบุตัวตนด้วยชื่อ (Display Name) เพื่อให้รู้ว่าใครเป็นคนสร้างงาน
- 📱 **Responsive UI: หน้าจอสวยงาม สะอาดตา ใช้งานได้ดีทั้งบนคอมพิวเตอร์และมือถือ

---

## 🛠 เทคโนโลยีที่ใช้ (Tech Stack)
- **Frontend: React.js, Tailwind CSS
- **Backend: Node.js, Express
- **Real-time Engine: Socket.io
- **Database: In-memory storage (ใช้ตัวแปรเก็บข้อมูลชั่วคราวใน RAM)

---

## 📋 สิ่งที่ต้องเตรียม (Prerequisites)
หากต้องการรันโปรเจกต์ในเครื่องตัวเอง (Local) จำเป็นต้องมี:
- **Node.js** (แนะนำเวอร์ชัน LTS)
- **Git** (สำหรับ Clone โปรเจกต์)

---

## 🚀 วิธีติดตั้งและรันโปรแกรมในเครื่อง (Local Installation)
เนื่องจากซอร์สโค้ดถูกจัดเก็บไว้ในโฟลเดอร์ `04_Implementation` เพื่อความเรียบร้อย โปรดทำตามขั้นตอนดังนี้:

### 1. ดาวน์โหลดโปรเจกต์ (Clone)
เปิด Terminal แล้วพิมพ์คำสั่ง:
```bash
git clone [https://github.com/tanapatkokw-dev/MytodoList.git](https://github.com/tanapatkokw-dev/MytodoList.git)
cd MytodoList
```

### 2. ติดตั้งและเตรียมหน้าเว็บ (Frontend Setup)
เราต้องคอมไฟล์ React ให้พร้อมใช้งานก่อน:
```bash
# เข้าไปที่โฟลเดอร์ client
cd 04_Implementation/src/client

# ติดตั้ง Library ที่จำเป็น
npm install

# สร้างไฟล์เว็บตัวจริง (Build)
npm run build
```

### ⚠️ ขั้นตอนสำคัญ: แก้ไข Code สำหรับรัน Local
ก่อนจะไปต่อ ต้องตรวจสอบไฟล์ client/src/App.js เพื่อให้เชื่อมต่อกับ Server ในเครื่องตัวเองได้ถูกต้อง:

1.เปิดไฟล์ `04_Implementation/src/client/src/App.js`

2.ค้นหาบรรทัด `const socket = io();`

3.แก้ไขให้ชี้ไปที่ `localhost:5000` (เพราะ `io()` เฉยๆ จะพยายามต่อไปที่ Port 3000 ของหน้าเว็บ ซึ่งผิด):
```bash
// ❌ ถ้าโค้ดเดิมเป็นแบบนี้ (ปกติใช้สำหรับ Production/Render)
// const socket = io(); 

// ✅ ให้แก้เป็นแบบนี้ (สำหรับรัน Local) เพื่อชี้ไปที่ Server Port 5000
const socket = io('http://localhost:5000'); 
```

### 3. รันเซิร์ฟเวอร์ (Backend Setup)
เมื่อแก้ไข Code และ Build หน้าเว็บเสร็จแล้ว ให้ถอยกลับไปรัน Server:
```bash
# ถอยออกมาและเข้าโฟลเดอร์ server
cd ../server

# ติดตั้ง Library ที่จำเป็น
npm install

# เริ่มต้นระบบ
npm start
```
✅ เมื่อรันสำเร็จจะขึ้นข้อความ: `SERVER RUNNING ON PORT 8000`

---

## วิธีการใช้งาน (Usage)
### วิธีที่ 1: ใช้งานผ่านเว็บออนไลน์ (แนะนำ)
เข้าลิงก์: https://mytodolist-qo86.onrender.com/

### วิธีที่ 2: ใช้งานผ่าน Localhost (สำหรับนักพัฒนา)
เมื่อรัน Server ตามขั้นตอนข้างต้นสำเร็จ ให้เข้าลิงก์: http://localhost:5000

### ขั้นตอนการใช้งาน:
- 1.กรอก ชื่อของคุณ (Acting as...) ในหน้าแรก
- 2.กดปุ่ม "เข้าสู่ระบบ" เพื่อเริ่มใช้งาน
- 3.(ทดสอบ Real-time): ลองเปิดลิงก์บน 2 แท็บ หรือเปิดบนมือถือและคอมพิวเตอร์พร้อมกัน จะเห็นว่าข้อมูลเชื่อมถึงกันทันที!

# ระบบจัดการงานแบบเรียลไทม์ (Real-time Collaborative To-Do List)

เว็บแอปพลิเคชันสำหรับจัดการรายการสิ่งที่ต้องทำ (To-Do List) ที่ออกแบบมาให้ผู้ใช้งานหลายคนทำงานร่วมกันได้ในเวลาเดียวกัน ข้อมูลจะอัปเดตหากันทันที (Real-time) โดยไม่ต้องกดรีเฟรชหน้าจอ เหมาะสำหรับการทำงานเป็นทีม

---

##  คุณสมบัติเด่น (Features)
* ** Real-time Synchronization:** เมื่อมีคนเพิ่ม, ลบ, หรือแก้ไขงาน หน้าจอของทุกคนจะเปลี่ยนตามทันที (ใช้เทคโนโลยี Socket.io)
* ** Multi-Status Tracking:** เปลี่ยนสถานะงานได้ 3 ระดับ (⏳ รอดำเนินการ, 🚧 กำลังทำ, ✅ เสร็จสิ้น) พร้อมสีแยกชัดเจน
* ** Edit & Delete:** แก้ไขข้อความและลบงานได้ง่ายๆ ด้วยปุ่มที่ออกแบบมาให้ใช้งานสะดวก (สีเหลือง/แดง)
* ** User Identification:** ระบบระบุตัวตนด้วยชื่อเล่น (Nickname) เพื่อให้รู้ว่าใครเป็นคนสร้างงาน
* ** Responsive UI:** หน้าจอสวยงาม สะอาดตา ใช้งานได้ดีทั้งบนคอมพิวเตอร์และมือถือ

---

##  เทคโนโลยีที่ใช้ (Tech Stack)
* **Frontend:** React.js, Tailwind CSS
* **Backend:** Node.js, Express
* **Real-time Engine:** Socket.io
* **Database:** In-memory storage (ใช้ตัวแปรเก็บข้อมูลชั่วคราวเพื่อสาธิตการทำงาน)

---

## 📋 สิ่งที่ต้องมีก่อน (Prerequisites)
โปรเจกต์นี้จำเป็นต้องใช้ **Node.js** ในการรัน หากยังไม่มีในเครื่อง กรุณาดาวน์โหลดและติดตั้งก่อนครับ:
* 👉 **ดาวน์โหลด Node.js:** [https://nodejs.org/](https://nodejs.org/) (แนะนำเวอร์ชัน LTS)
* *วิธีเช็ค:* เปิด Terminal พิมพ์ `node -v` ถ้าขึ้นตัวเลขเวอร์ชันแสดงว่าพร้อมใช้งานแล้ว

---

## วิธีติดตั้งและรันโปรเจกต์ (Installation & Run)

เนื่องจากซอร์สโค้ดถูกจัดเก็บไว้ในโฟลเดอร์ `04_Implementation` เพื่อความเป็นระเบียบ โปรดทำตามขั้นตอนดังนี้:

### 1. ดาวน์โหลดโปรเจกต์ (Clone)
เปิด Terminal แล้วพิมพ์คำสั่ง:
```bash
git clone https://github.com/tanapatkokw-dev/MytodoList.git
```
### 2. ติดตั้งและสร้างหน้าเว็บ (Frontend Setup)
เราต้องแปลงโค้ด React ให้พร้อมใช้งานก่อน

```bash
# เข้าไปที่โฟลเดอร์ client
cd 04_Implementation/src/client

# ติดตั้ง Library ที่จำเป็น
npm install

# สร้างไฟล์เว็บตัวจริง (Build)
npm run build
```
### 3.รันเซิร์ฟเวอร์ (Backend Setup)
เมื่อสร้างหน้าเว็บเสร็จแล้ว ให้ถอยกลับไปรัน Server
```Bash
# ถอยออกมาและเข้าโฟลเดอร์ server
cd ../server

# ติดตั้ง Library ที่จำเป็น
npm install

# เริ่มต้นระบบ
npm start
```
เมื่อรันสำเร็จจะขึ้นข้อความ: `SERVER RUNNING ON PORT 5000`

## วิธีเข้าใช้งาน (Usage)
เมื่อรัน Server สำเร็จแล้ว

เปิด Google Chrome แล้วเข้าลิงก์:

👉 http://localhost:5000

ตั้งชื่อ Nickname แล้วกด Join Room เพื่อเริ่มใช้งาน

(ทดสอบ Real-time): ลองเปิด Tab ใหม่ หรือเปิดในโหมด Incognito แล้วเข้าลิงก์เดิม จะเห็นว่าข้อมูลเชื่อมถึงกันทันที!

## 📂 โครงสร้างไฟล์ (Code Structure)

โครงสร้างไดเรกทอรีของโปรเจกต์มีการจัดระเบียบดังนี้:

```text
MytodoList/
├── README.md               # คู่มือโปรเจกต์ (ไฟล์นี้)
├── 01_Requirements/        # เอกสาร Requirement และ User Stories
├── 04_Implementation/      # Source Code หลักของโปรแกรม
│   └── src/
│       ├── server/         # Backend (Node.js + Socket.io)
│       │   ├── index.js    # จุดเริ่มต้น Server (Entry Point)
│       │   └── package.json
│       │
│       └── client/         # Frontend (React)
│           ├── public/
│           └── src/
│               ├── App.js  # หน้าจอหลักและ Logic การทำงาน
│               └── index.css
│
├── 05_Testing/             # เอกสาร Test Case
└── 06_Deployment_Review/   # คู่มือการใช้งาน (User Manual)
```

## ข้อควรระวัง (Important Notes)

* **ข้อมูลชั่วคราว (Temporary Data):**
    ข้อมูลงานทั้งหมดถูกจัดเก็บอยู่ใน **RAM** ของ Server (In-Memory Database) ดังนั้นหากมีการปิดหรือ Restart Server **ข้อมูลทั้งหมดจะสูญหายทันที**

* **การเชื่อมต่อ (Connection):**
    จำเป็นต้องเปิดหน้าต่าง **Terminal** ที่รัน Server ทิ้งไว้ตลอดเวลาในขณะที่ใช้งานแอปพลิเคชัน

# เอกสารขอบเขตของระบบ (System Scope Document)

**Project:** Real-time Collaborative To-Do List
**Version:** 1.0 (Based on Current Implementation)
**Last Updated:** 2025-11-30

---

## 1. ภาพรวมของระบบ (System Overview)
ระบบนี้คือเว็บแอปพลิเคชันจัดการงาน (Task Management) ที่เน้นการทำงานร่วมกันแบบ **Real-time** ผ่านเทคโนโลยี Socket.io ผู้ใช้งานสามารถเข้าสู่ระบบด้วยชื่อสมมติ (Display Name) และเห็นการเปลี่ยนแปลงของสถานะงานทันทีที่เพื่อนร่วมทีมทำการแก้ไข โดยข้อมูลจะอัปเดตอัตโนมัติโดยไม่ต้องรีเฟรชหน้าจอ

### ผู้ใช้งานระบบ (Actors)
* **User (Collaborator):** ผู้ใช้งานทั่วไปที่เข้าสู่ระบบ โดยระบุชื่อ (Acting as...) เพื่อบ่งบอกตัวตน ทุกคนมีสิทธิ์เท่าเทียมกันในการ สร้าง, แก้ไข, ลบ และเปลี่ยนสถานะงาน

---

## 2. ขอบเขตของฟังก์ชัน (Functional Requirements)
อ้างอิงจาก Source Code ปัจจุบัน ระบบมีความสามารถดังนี้:

### **FR-01: การระบุตัวตน (Identification)**
* **Login Screen:** ผู้ใช้ต้องระบุชื่อ (Display Name) ในหน้า Welcome Screen ก่อนเข้าสู่หน้า Dashboard
* **Identity Display:** ชื่อที่ระบุจะถูกนำไปแสดงในฟิลด์ `createdBy` ของงานที่สร้างขึ้น และแสดงสถานะ "Acting as: [ชื่อ]" ที่ส่วนหัวของโปรแกรม

### **FR-02: การจัดการงาน (Task Operations)**
* **Create (สร้าง):** * ผู้ใช้สามารถเพิ่มงานใหม่ได้ผ่าน Input box
    * ระบบจะสร้าง ID (UUID) และบันทึกเวลา (Timestamp) ให้โดยอัตโนมัติ
* **Read (อ่าน):** * แสดงรายการงานทั้งหมดเรียงตามลำดับการสร้าง
    * แสดงรายละเอียดครบถ้วน: ชื่องาน, ผู้สร้าง, เวลาที่สร้าง, และสถานะปัจจุบัน
* **Update (แก้ไข):**
    * **Edit Title:** ผู้ใช้สามารถกดปุ่ม `[Edit]` เพื่อแก้ไขข้อความชื่องานได้ (Inline Edit)
    * **Change Status:** เปลี่ยนสถานะได้ 3 ระดับผ่าน Dropdown menu ซึ่งจะมีผลต่อสีของ UI ทันที:
        1.  `todo` (⏳ รอดำเนินการ - สีเทา)
        2.  `in_progress` (🚧 กำลังทำ - สีส้ม)
        3.  `done` (✅ เสร็จสิ้น - สีเขียว/ขีดฆ่า)
* **Delete (ลบ):** * ลบงานออกจากระบบผ่านปุ่ม `[Del]`
    * มีระบบแจ้งเตือนยืนยัน (Confirmation Alert) ก่อนลบจริงเพื่อป้องกันความผิดพลาด

### **FR-03: การทำงานแบบ Real-time (Synchronization)**
* **Instant Broadcast:** ทุกการกระทำ (addTask, updateStatus, editTask, deleteTask) ทาง Server จะส่งข้อมูลล่าสุดให้ Client ทุกเครื่องทันที
* **Live UI Update:** หน้าจอของผู้ใช้อื่นจะเปลี่ยนแปลงสี ข้อความ และสถานะตามข้อมูลล่าสุดโดยอัตโนมัติโดยไม่ต้องกด Refresh

---

## 3. ขอบเขตข้อมูล (Data Scope & Model)
ปัจจุบันระบบจัดเก็บข้อมูลแบบ **In-Memory** (RAM) ข้อมูลจะดำรงอยู่ตราบเท่าที่ Server ยังทำงาน

**โครงสร้างข้อมูล (Task Object Structure):**
อ้างอิงจากไฟล์ `index.js` และการใช้งานใน `App.js`:

```json
{
  "id": "uuid-v4-generated",      // รหัสอ้างอิงงาน (Unique ID)
  "title": "String",              // ชื่องาน
  "status": "todo" | "in_progress" | "done", // สถานะงาน (Enum)
  "createdBy": "String",          // ชื่อผู้สร้าง (จาก User Input)
  "createdAt": Number             // Timestamp (Date.now())
}
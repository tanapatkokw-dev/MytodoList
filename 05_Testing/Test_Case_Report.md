# รายงานผลการทดสอบระบบ (Test Case Report)

**Project Name:** Real-time Collaborative To-Do List
**Version:** 1.0

---



## 1. สรุปผลการทดสอบ (Test Summary)
* **Total Test Cases:** 12
* **Pass:** 12
* **Fail:** 0

---

## 2. รายละเอียดกรณีการทดสอบ (Test Case Details)

### 2.1 ฟังก์ชันการระบุตัวตน (Authentication)

| ID | ชื่อการทดสอบ | ประเภท | สิ่งที่ต้องมีก่อน (Pre-condition) | ข้อมูลนำเข้า/การกระทำ (Input/Action) | ผลลัพธ์ที่คาดหวัง (Expected Output) | ผลลัพธ์จริง (Actual Output) | สถานะ |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| **TC-01** | **เข้าสู่ระบบสำเร็จ** | Success | อยู่หน้า Login | 1. กรอกชื่อ "UserA"<br>2. กดปุ่ม "เข้าสู่ระบบ" | ระบบซ่อนหน้า Login และแสดงหน้า Dashboard พร้อมระบุ "Acting as: UserA" | ตรงตามคาดหวัง | Pass |
| **TC-02** | **ป้องกันชื่อว่าง (Empty Name)** | Error | อยู่หน้า Login | 1. ปล่อยช่องชื่อว่างไว้<br>2. กดปุ่ม "เข้าสู่ระบบ" | ปุ่มกดไม่ตอบสนอง ไม่มีการเปลี่ยนหน้าจอ (เนื่องจาก Logic `user && setIsJoined(true)`) | ตรงตามคาดหวัง | Pass |

### 2.2 ฟังก์ชันจัดการงาน (Task Management)

| ID | ชื่อการทดสอบ | ประเภท | สิ่งที่ต้องมีก่อน (Pre-condition) | ข้อมูลนำเข้า/การกระทำ (Input/Action) | ผลลัพธ์ที่คาดหวัง (Expected Output) | ผลลัพธ์จริง (Actual Output) | สถานะ |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| **TC-03** | **เพิ่มงานใหม่สำเร็จ** | Success | Login แล้ว | 1. พิมพ์ "ส่งงานลูกค้า"<br>2. กด Enter หรือปุ่ม Add Task | รายการ "ส่งงานลูกค้า" ปรากฏท้ายสุด สถานะเป็น "รอดำเนินการ" และ Input ถูกเคลียร์ค่า | ตรงตามคาดหวัง | Pass |
| **TC-04** | **ป้องกันงานว่าง (Empty Task)** | Boundary | Login แล้ว | 1. ปล่อยช่อง Input ว่าง<br>2. กดปุ่ม Add Task | ระบบไม่สร้างงานใหม่ และไม่ส่งข้อมูลไป Server (ติดเงื่อนไข `input.trim()`) | ตรงตามคาดหวัง | Pass |
| **TC-05** | **เพิ่มงานด้วย Spacebar (Whitespace)** | Boundary | Login แล้ว | 1. เคาะ Spacebar 3 ครั้ง<br>2. กดปุ่ม Add Task | ระบบไม่สร้างงานใหม่ (เนื่องจาก `trim()` ตัดช่องว่างออกจนหมด) | ตรงตามคาดหวัง | Pass |
| **TC-06** | **เปลี่ยนสถานะเป็น Done** | Success | มีงานสถานะ Todo | 1. เปลี่ยน Dropdown เป็น "เสร็จสิ้น" | 1. กรอบงานเปลี่ยนเป็นสีเขียว<br>2. ชื่องานมีเส้นขีดฆ่า (Line-through css) | ตรงตามคาดหวัง | Pass |
| **TC-07** | **แก้ไขชื่องาน (Edit)** | Success | มีงานอยู่ | 1. กด `[Edit]`<br>2. แก้ไขชื่อเป็น "แก้ไขแล้ว"<br>3. กด `[Save]` | ข้อความเปลี่ยนเป็น "แก้ไขแล้ว" และออกจากโหมดแก้ไข | ตรงตามคาดหวัง |  Pass |
| **TC-08** | **บันทึกการแก้ไขด้วยชื่อว่าง** | Error | อยู่ในโหมด Edit | 1. ลบข้อความออกจนหมด<br>2. กด `[Save]` | ระบบไม่บันทึกและยังคงค้างอยู่ในโหมด Edit (ติดเงื่อนไข `editText.trim()`) | ตรงตามคาดหวัง | Pass |
| **TC-09** | **ลบงานแบบยืนยัน (Confirm)** | Success | มีงานอยู่ | 1. กด `[Del]`<br>2. กดปุ่ม "OK" ที่ Browser Alert | รายการงานหายไปจากหน้าจอ | ตรงตามคาดหวัง | Pass |
| **TC-10** | **ลบงานแบบยกเลิก (Cancel)** | Boundary | มีงานอยู่ | 1. กด `[Del]`<br>2. กดปุ่ม "Cancel" ที่ Browser Alert | หน้าต่าง Alert ปิดลง และรายการงานยังคงอยู่เหมือนเดิม | ตรงตามคาดหวัง | Pass |

### 2.3 ฟังก์ชันการทำงานร่วมกัน (Real-time Synchronization)

| ID | ชื่อการทดสอบ | ประเภท | สิ่งที่ต้องมีก่อน (Pre-condition) | ข้อมูลนำเข้า/การกระทำ (Input/Action) | ผลลัพธ์ที่คาดหวัง (Expected Output) | ผลลัพธ์จริง (Actual Output) | สถานะ |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| **TC-11** | **Sync การเพิ่มงาน (Multi-user)** | Real-time | เปิด Browser A และ B | 1. User A เพิ่มงาน "Meeting"<br>2. ดูหน้าจอ User B | หน้าจอ User B แสดงงาน "Meeting" ขึ้นมาทันทีโดยไม่ต้อง Refresh | ตรงตามคาดหวัง | Pass |
| **TC-12** | **Sync การเปลี่ยนสีสถานะ** | Real-time | งานชื่อ "Job1" อยู่บน A และ B | 1. User A เปลี่ยนสถานะเป็น "In Progress" | หน้าจอ User B เปลี่ยนสีกรอบงาน "Job1" เป็น **สีส้ม** ทันที | ตรงตามคาดหวัง | Pass |

---

## 3. หมายเหตุ (Notes)
* การทดสอบนี้ทำบนสภาพแวดล้อม Localhost
* ข้อมูลทั้งหมดถูกเก็บใน Memory (RAM) หาก Restart Server ข้อมูลจะหายไป ซึ่งเป็นพฤติกรรมปกติของเวอร์ชันนี้
